/**
 * Grupos 4 y 5 — Gráfico 3: distancia vs tiempo (Plotly.js).
 * Props: GraphProps { result, currentFrame }
 *
 * TODO: Implementar por los Grupos 4/5.
 * El componente recibe result y currentFrame como props.
 * Curva distance vs time, con cursor vertical en time[currentFrame],
 * marcando punto de distancia mínima e intercepción.
 */
import type { GraphProps } from "../shared/types";

export default function DistancePlot({ result, currentFrame }: GraphProps) {
  const _r = result;
  const _f = currentFrame;
  void _r;
  void _f;
  return null; // Placeholder — Grupos 4 y 5
}

