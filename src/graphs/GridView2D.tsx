/**
 * Grupos 4 y 5 — Gráfico 1: vista cenital 2D con grilla (Plotly.js).
 * Props: GraphProps { result, currentFrame }
 *
 * TODO: Implementar por los Grupos 4/5.
 * El componente recibe result y currentFrame como props.
 * Dibujar trayectorias aircraft.position y missile.position en XY,
 * con marcadores en currentFrame y rastro hasta currentFrame.
 */
import type { GraphProps } from "../shared/types";

export default function GridView2D({ result, currentFrame }: GraphProps) {
  const _r = result;
  const _f = currentFrame;
  void _r;
  void _f;
  return null; // Placeholder — Grupos 4 y 5
}

