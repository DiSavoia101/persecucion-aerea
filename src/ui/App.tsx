/**
 * Grupo 3 — UI / orquestador.
 * Pestañas, layout, integración de controles, reproducción y gráficos.
 *
 * Actualmente muestra GraphsPreview (gráficos 2D con mockResult)
 * mientras el Grupo 3 implementa el reproductor y los controles completos.
 */
import GraphsPreview from "./GraphsPreview";

export default function App() {
  return <GraphsPreview />;
}
